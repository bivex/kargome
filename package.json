{
  "name": "kargome",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "tokens": "node scripts/build-tokens.js",
    "dev": "bun run tokens && vite",
    "build": "bun run tokens && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "lit": "^3.2.0",
    "lucide": "^0.474.0"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "lightningcss": "^1.28.0"
  }
}
