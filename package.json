{
  "name": "kargome",
  "version": "0.1.0",
  "private": true,
  "description": "Lit + Vite + LightningCSS web components boilerplate",
  "license": "MIT",
  "type": "module",
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "tokens": "node scripts/build-tokens.js",
    "dev": "bun run tokens && vite",
    "build": "bun run tokens && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "lit": "^3.2.0",
    "lucide": "^0.474.0"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "lightningcss": "^1.28.0"
  }
}
