#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "📤 Pre-push build check..."
bun run build

if [ $? -ne 0 ]; then
  echo "❌ Build failed. Please fix the errors before pushing."
  exit 1
fi

echo "✅ Ready to push!"
